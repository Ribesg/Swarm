import {
    lineHeight,
    monoFont,
    sansFont,
} from "font";

const axisTickFontFamily = monoFont;
const axisTitleFontFamily = sansFont;
const chartTitleFontFamily = sansFont;
const legendFontFamily = sansFont;
const tooltipFontFamily = sansFont;

export {
    axisTickFontFamily,
    axisTitleFontFamily,
    chartTitleFontFamily,
    legendFontFamily,
    lineHeight,
    tooltipFontFamily,
};
